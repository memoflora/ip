# AI Usage

**Tool used:** Claude Code

## Usage

- **Standardising GUI specifications** — Used to define and maintain consistent UI styling across the application, such as colour schemes, font sizes, and layout properties.
- **Generating Javadoc comments** — Used to add Javadoc documentation to methods and classes, as writing them manually is repetitive and error-prone when done at scale.
